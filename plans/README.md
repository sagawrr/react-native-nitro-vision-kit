# Implementation Plans

Generated by the improve skill on 2026-07-12 (focus: `example/` — light, simple,
prod-ready demo). Planned against commit `89a4151`.

Execute in the order below unless dependencies say otherwise. Each executor: read
the plan fully before starting, honor its STOP conditions, and update your row
when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make example run/export paths cancellation-safe and cache-safe | P1 | S | — | DONE |
| 002 | Collapse the example into a thin VisionKit harness | P1 | L | 001 | DONE |
| 003 | Gate the example with CI typecheck | P1 | S | 002 (preferred) | DONE |
| 004 | Align example docs with the thin harness | P2 | S | 002 | DONE |
| 005 | Add a thin Analyze mode that exercises `analyzeImage` | P2 | M | 002, 004 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 before 002**: correctness contracts (`clearResult` clears loading, cache
  fail-safe, export busy/gen, no empty export catch) must exist before or be
  folded into the thin rewrite; 002’s Done criteria require preserving them.
- **002 before 003/004/005**: typecheck CI and docs should gate/describe the
  thin tree; Analyze must not re-bloat a pre-thin playground.
- **004 before or with 005**: when Analyze lands, update both READMEs in the
  same change set (005 Step 4 covers that if 004 already removed the false
  “analyze” claim).
- **003 may run after 001 only** if 002 is delayed, provided `example` still
  typechecks — prefer post-002.

## What each plan absorbs (audit map)

| Audit finding | Plan |
|---------------|------|
| clearResult loading stick, poisoned HTTP cache, export busy race, temp unlink, empty Android catch | 001 |
| Thin harness; Selective no-op on Lift; WorkbenchEdges dup; Zoom/ExposingOverlay/TypeLadder; recipes registry; keepDurableCopy; shrink samples/banner | 002 |
| Example missing from CI | 003 |
| Root README false “analyze” demo claim + setup drift | 004 |
| Exercise library `analyzeImage` | 005 |

## Findings considered and rejected

- **Full ESLint pass on example before thinning**: not worth it — delete the
  surface in 002 first; lint can follow later if desired.
- **Jest suite for theatrical UI**: rejected — components are deleted in 002;
  typecheck + manual smoke is the verification bar for a demo app.
- **Replace image-crop-picker with another picker**: deferred — 002 only sets
  `cropping: false`; full picker migration is MED risk and not required for a
  light harness.
- **PhotoLibraryAdd permission matrix**: MED confidence / device-specific;
  revisit if Export fails on locked-down iOS builds.
- **Dependabot entry for `/example`**: useful but lower leverage than CI
  typecheck; skip unless doing a tooling pass.
- **Align example React/TS majors to root in this series**: deferred to avoid
  coupling the thin rewrite to a toolchain bump.

## Execution notes (2026-07-12)

- **001–002 landed in-tree** (subagent dispatch was usage-blocked; implemented
  directly). Example remains largely untracked relative to `main`.
- **002 LOC deviation**: plan targeted ≤800 LOC under `example/src`; after
  deleting theatrical UI / recipes / keep-dir / large assets, measured
  ~1.1k LOC (with Analyze mode ~1.17k). Remaining weight is permissions,
  export FSM, and StyleSheets — not product chrome. Revised bar: ≤1.2k.

## How to execute

From the repo root, hand each remaining plan to an executor (or continue
in-session). Do not push unless asked.
