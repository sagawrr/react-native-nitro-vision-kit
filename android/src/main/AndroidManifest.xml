<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <!-- Download ML Kit subject-segmentation + image-labeling + Latin OCR models after app install -->
    <meta-data
      android:name="com.google.mlkit.vision.DEPENDENCIES"
      android:value="subject_segment,image_labeling,ocr" />
  </application>
</manifest>
